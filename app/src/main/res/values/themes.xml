<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.CatClinic" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/pastel_coral</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
  <!-- Base application theme. -->
    <style name="Base.Theme.CatClinic" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>


    <!-- CatClinicButton style for all four homepage buttons -->
    <style name="CatClinicButton" parent="Widget.MaterialComponents.Button">
        <!-- nice rounded corners -->
        <item name="cornerRadius">24dp</item>
        <!-- keep text human-readable -->
        <item name="android:textAllCaps">false</item>
        <item name="android:textColor">@color/nearBlack</item>
        <!-- icon on the left of the text -->
        <item name="iconGravity">textStart</item>
        <!-- comfortable touch targets -->
        <item name="android:padding">12dp</item>
    </style>
</resources>